1. You can access the status code of a requests response object using the `status_code` attribute.
2. Use the `assert` statement in Python to verify the condition is true.
3. Your assertion should look like this: `assert response.status_code == 200`.
