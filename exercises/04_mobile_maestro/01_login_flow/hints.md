1. Maestro uses `tapOn` to interact with elements by their text or accessibility ID.
2. Use `inputText` to simulate typing into a text field.
3. For example, `- tapOn: "Username"` followed by `- inputText: "user1"`.
