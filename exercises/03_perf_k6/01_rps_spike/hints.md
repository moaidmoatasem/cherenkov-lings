1. A spike test in k6 is configured using the `stages` option.
2. You want a rapid increase in Virtual Users (VUs), a sustain period, and a rapid decrease.
3. Use the `check` function from `k6` to verify the response status is 200.
