## Hint 1 (Architectural Nudge)
Provider verification ensures the server continues to fulfill contracts generated by all active consumers.

## Hint 2 (API Pattern)
Loop over orders and assert on data types (`str`, `float`) and allowed status enums.

## Hint 3 (Code Diff)
```diff
+ for order in data["orders"]:
+     assert isinstance(order["id"], str)
+     assert order["currency"] == "USD"
```
